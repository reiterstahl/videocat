{
  "name": "videocat",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "AGPL-3.0-or-later",
  "description": "Offline video catalog for external hard drives.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @videocat/shared && npm run build -w @videocat/server && npm run build -w @videocat/web && npm run build -w @videocat/agent-windows",
    "dev:server": "npm run dev -w @videocat/server",
    "dev:web": "npm run dev -w @videocat/web",
    "typecheck": "npm run build -w @videocat/shared && npm run typecheck -w @videocat/server && npm run typecheck -w @videocat/web && npm run typecheck -w @videocat/agent-windows",
    "prisma:generate": "npm run prisma:generate -w @videocat/server",
    "prisma:migrate": "npm run prisma:migrate -w @videocat/server",
    "test:unit": "tsx --test tests/unit/*.test.ts",
    "test:api": "tsx --test tests/api.test.ts",
    "test:coverage": "tsx --test --experimental-test-coverage --test-coverage-lines=90 --test-coverage-branches=70 --test-coverage-functions=95 tests/unit/pin-security.test.ts tests/unit/path-security.test.ts tests/unit/file-transfer.test.ts",
    "test": "npm run build -w @videocat/shared && npm run prisma:generate && npm run test:unit && npm run test:api && npm run test:coverage"
  },
  "devDependencies": {
    "tsx": "^4.22.4"
  },
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  }
}
